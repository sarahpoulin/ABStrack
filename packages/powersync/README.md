# powersync

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build powersync` to build the library.

## Running unit tests

Run `nx test powersync` to execute the unit tests via [Vitest](https://vitest.dev/).
